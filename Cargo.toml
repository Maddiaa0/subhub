[package]
name = "sub-manager"
version = "0.1.0"
edition = "2024"
description = "Manage named Claude Code OAuth credentials in the macOS Keychain"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

